<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowTranslucentStatus">true</item>

        <!--ActionMode-->
        <item name="android:windowActionModeOverlay">true</item>
        <item name="windowActionModeOverlay">true</item> <!--Overlay toolbar-->
        <item name="android:actionModeBackground">@color/color_actionmode</item>
        <item name="actionModeBackground">@color/color_actionmode</item>
        <item name="android:actionModeCloseDrawable">@mipmap/ic_arrow_back_white</item>

    </style>

</resources>
