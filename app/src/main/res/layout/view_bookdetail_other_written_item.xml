<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@color/color_white"
    android:paddingStart="@dimen/inner_item_padding"
    android:paddingEnd="@dimen/inner_item_padding"
    android:paddingBottom="@dimen/inner_item_padding">

    <ImageView
        android:id="@+id/book_cover"
        android:layout_width="@dimen/book_cover_width"
        android:layout_height="@dimen/book_cover_height"
        android:layout_alignParentStart="true"
        android:layout_alignParentTop="true"
        android:scaleType="fitXY"/>

    <TextView
        android:id="@+id/book_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_toEndOf="@+id/book_cover"
        android:textSize="16sp"
        android:layout_marginStart="18dp"
        android:layout_marginTop="8dp"/>

    <TextView
        android:id="@+id/book_latest_chapter"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignStart="@+id/book_name"
        android:layout_below="@+id/book_name"
        android:layout_marginTop="20dp"
        android:maxLines="2"
        android:ellipsize="end"/>

</RelativeLayout>
